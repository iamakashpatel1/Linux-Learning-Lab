Day-02 Linux File System

File Hierarchy System (FHS)
/bin - contain binary command for all users
/sbin - system binary command for all users (we don't need to create command just need to run those which are already in system)
/root - is the root directory for users
/boot - this directory contain all the file and requirement this for bootloader.
/var - this directory contain all the log files , user mailbox.
/dev - this directory contain the hardware information of system
/temp - is the directory that contain temperory file.
/proc - this contain the information about the process and system
/sys - contain information about device and drive.


### What I learned today..
1. which <command> - which look for the directory from where the command is execuated.
2. whereis <command> - look for the full path for the file or directory from where the command is execuated.
3. find /home -name file.txt - find the location  of a file in home directory and through all the home directory
4. locate file.txt - also finds the index of the file.


## Some great utilities or commands
1. grep -i word file.txt - finds the line that contain that word.
2. egrep or grep-E - is use to look in error files to look for troubleshoot.
3. diff file1.txt file2.txt - finds the difference between two files.


####  Linu Boot Process  ###
1. Hardware boot phase
2. Bootloader phase
3. Kernel phase

1. When the system is power on the BIOS check the hardware weather the hardware are working good or not.
2. Then the control is passed to bootloader which load the require code to boot the system.
3. Boatloader takes the part of RAM disk and load the code(initrd or initramf)
4. Then it load basic OS and specific hardware drivers then the control is passed to kernel.
5. Now the kernel uses the RAM disk to start the OS and load daemon and other program.
6. Then the kernel dismounts and distroy the RAM disk to be used for system memory.
7. At the last kernel initialize system and load systemd

